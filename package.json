{
	"name": "obsidian-url-preview",
	"version": "0.3.0",
	"description": "Preview external URLs on hover. Works in Editor, Live Preview, and Reader modes.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "npm run lint && tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"lint": "eslint . --max-warnings 0",
		"lint:fix": "eslint . --fix",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": ["obsidian", "obsidian-plugin", "obsidian-url-preview", "url-preview"],
	"author": "ovitrif",
	"license": "MIT",
	"devDependencies": {
		"@eslint/js": "^9.30.1",
		"@types/node": "^20.12.12",
		"esbuild": "^0.17.3",
		"eslint": "^9.30.1",
		"eslint-plugin-obsidianmd": "0.1.9",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "~5.4.5",
		"typescript-eslint": "^8.35.1"
	}
}
